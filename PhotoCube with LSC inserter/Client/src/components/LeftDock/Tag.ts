/**
 * Interface representing a tag that can be used as a filter.
 * Displayed in a dropdown as tagName in TagSearcher.
 * Based on PublicTag.cs at the server side.
 */
 export interface Tag {
    Id: number,
    Name: string
}
